# Good code

## Keep long lines intact up to 240 characters, except streams and builders

```java
shipmentService.recordShipment(customerOrder, shippingProvider, shipmentTrackingNumber, warehouseLocation, shipmentDispatchedAt, estimatedDeliveryDate, shippingNotificationPreferences);
```
